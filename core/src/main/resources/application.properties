spring.application.name=core
spring.profiles.active=local

spring.datasource.url=jdbc:postgresql://aws-1-ap-south-1.pooler.supabase.com:6543/postgres?user=postgres.xrcyhvcogjpbjtaiihst&password=${supabase_password}&sslmode=require&prepareThreshold=0
spring.datasource.hikari.data-source-properties.cachePrepStmts=false
spring.datasource.username=${supabase_uid}
spring.datasource.password=${supabase_password}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
# Hibernate-specific settings
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.use_sql_comments=true

server.port=8081

# IAM Service Configuration - must match IAM server.port (8080)
iam.service.url=http://localhost:8080
iam.organization.url=/iam/organizations
iam.user.url=/iam/users
iam.auth.url=/iam/auth

# Core Service URLs - must match core server.port (8081)
core.service.url=http://localhost:8081
core.products.url=/core/products
core.materials.url=/core/materials
core.warehouses.url=/core/warehouses
core.orders.url=/core/orders
core.partnerships.url=/core/partnerships

# DMS Service URLs
dms.service.url=http://localhost:8083
individual.dms.url=/dms/upload/individual
org.dms.url=/dms/upload/org
common.dms.url=/dms/upload/common

# HR Service URLs
hr.service.url=http://localhost:8084
hr.init.url=/hr/init
hr.employee.directory.url=/hr/employees/directory
hr.employee.details.url=/hr/employees/details
hr.employee.approval.authority.url=/hr/employee/approval-authority
hr.employee.details.by.id.url=/hr/employee/details/{employeeId}

# CMS Service URLs
cms.service.url=http://localhost:8085
cms.conversation.url=/cms/conversations
cms.message.url=/cms/messages

# PMS Service URLs
pms.service.url=http://localhost:8086

# Keycloak Configuration
keycloak.server-url=http://localhost:9090
keycloak.realm=nexus
keycloak.client-id=core-client
keycloak.client-secret=

# OAuth2 Resource Server - JWT validation (mirrors iam: application.properties:52)
spring.security.oauth2.resourceserver.jwt.jwk-set-uri=http://localhost:9090/realms/nexus/protocol/openid-connect/certs
spring.security.oauth2.resourceserver.jwt.issuer-uri=http://localhost:9090/realms/nexus

# Generic user for service-to-service calls
generic.user.id=service@nexus.com
generic.password=service123

# Token validation endpoints (via IAM) - must match IAM server.port
verify.token.url=http://localhost:8080/iam/auth/verify
generate.token.url=http://localhost:8080/iam/auth/login
decrypt.token.url=http://localhost:8080/iam/auth/decrypt

# Purchase Order Approval Thresholds (FR-RET-002)
po.approval.threshold.auto=10000
po.approval.threshold.manager=50000
po.approval.threshold.director=100000